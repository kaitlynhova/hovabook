<%= form_for task, :remote => true do |f| %>
  <%= f.check_box :done %>
  <%= f.submit "Update" %>
  <%= f.label :done, task.descritpion %>
  <%= link_to "(remove)", task, method: :delete, data: {confirm: "Are you sure?"}, :remote => true %>
<% end %>
