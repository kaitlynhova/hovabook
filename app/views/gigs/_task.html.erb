<%= form_for task, :remote => true do |f| %>
  <%= f.check_box :done , :class => "todo_check"%>
  <%= f.label :done, task.descritpion %>
  <%= link_to "REMOVE", task, method: :delete, data: {confirm: "Are you sure, dawg?"}, :remote => true %>
<% end %>
