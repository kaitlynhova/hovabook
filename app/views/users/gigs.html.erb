<h1>MY GIGS</h1>

<div >
  <% @gigs.each do |gig| %>
      <div class="small-4 large-4 columns">
    
        <%= image_tag gig.image_url(:box), :class => "center-it" if gig.image? %>
    
      </div>
        
  <% end %>
</div>